sudo: required

language: ruby

services:
  - docker

before_install:
  - docker build -t duncdrum/exist-docker .
script:
  - docker run -it -d -p 8080:8080 duncdrum/exist-docker
  - docker log duncdum/exist-docker
  - docker ps -a
